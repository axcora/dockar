<div class="container-fluid">
<div class="col-md-8 mx-auto p-3 p-md-5 text-center border rounded">
<h3><strong>{{metadata.support.title}}</strong></h3>
<p>{{metadata.support.text}}</p>
<div class="mt-4">
{% for p in metadata.support.button %}
<a href="{{p.link}}" class="btn bg-body-tertiary shadow-sm me-1 ms-1"><i class="bi bi-{{p.icon}}"></i> {{p.name}}</a>
{% endfor %}
</div>
</div>
</div>